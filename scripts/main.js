'use strict';

function writeThis(word, number) {
    for (let i = 0; i < number; i++) {
        console.log(word);
    }

}

writeThis('patata', 10);
writeThis('aguacate', 7);
writeThis('Pizza', 50);